# ===========================================
# Flow Guardian Environment Configuration
# ===========================================
# Copy this to .env and fill in your values

# Cerebras API (Required for LLM synthesis)
# Get your key at: https://cloud.cerebras.ai
CEREBRAS_API_KEY=your-cerebras-api-key-here

# ===========================================
# Embeddings Configuration (for semantic search)
# ===========================================
# Gemini API (Recommended - fast, free tier)
# Get your key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Embedding provider: "auto" (default), "gemini", or "local"
# - auto: Uses Gemini if API key set, else local
# - gemini: Forces Gemini (requires GEMINI_API_KEY)
# - local: Forces local sentence-transformers (requires torch)
EMBEDDING_PROVIDER=auto

# ===========================================
# Local Memory (Backboard Replacement)
# ===========================================
# Set to true to use local vector storage instead of Backboard
USE_LOCAL_MEMORY=true

# Vector database path (default: ~/.flow-guardian/vectors.db)
# FLOW_GUARDIAN_DB_PATH=~/.flow-guardian/vectors.db

# ===========================================
# Backboard.io API (Optional - legacy/fallback)
# ===========================================
# Only needed if USE_LOCAL_MEMORY=false
# BACKBOARD_API_KEY=your-backboard-api-key-here
# BACKBOARD_BASE_URL=https://app.backboard.io/api
# BACKBOARD_PERSONAL_THREAD_ID=
# BACKBOARD_TEAM_THREAD_ID=

# User identity (for team learnings attribution)
FLOW_GUARDIAN_USER=your-name
