# Core dependencies
cerebras-cloud-sdk>=1.0.0
httpx>=0.25.0
rich>=13.0.0
click>=8.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.0

# Local Vector Storage (Backboard replacement)
sqlite-vec>=0.1.1
numpy>=1.24.0

# Embeddings - Gemini (default, lightweight)
google-genai>=1.0.0

# Embeddings - Local (optional, heavier but works offline)
# Uncomment if you want offline embeddings:
# sentence-transformers>=2.2.0
# torch>=2.0.0

# HTTP API (FastAPI)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# MCP Server
mcp>=1.0.0
starlette>=0.35.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
mypy>=1.0.0
ruff>=0.1.0
